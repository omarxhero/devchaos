# Keep LF in the working tree on every platform: renderer string-patches match
# exact multi-line shader needles, and CRLF checkouts break them silently.
* text=auto eol=lf
*.png binary
*.gif binary
*.jpg binary
*.ico binary
*.wav binary
